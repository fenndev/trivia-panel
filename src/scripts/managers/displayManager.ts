class DisplayManager {
    // Global Variables
    public body: HTMLElement = document.body;

    public displaySplashScreen() {
    
        // // Add Event Listeners
        // splashScreenButton.addEventListener('click', () => {
        //     this.hideSplashScreen();
        //     this.showCategoryScreen(categories);
        // });
        // this.body.addEventListener('keydown', function(e) {
        //     e.preventDefault();
        //     if(e.key === 'Escape') {
        //         this.hideSplashScreen();
        //         this.showCategoryScreen(categories);
        //         this.removeEventListener('keydown', arguments.callee);
        //     }
        // });
    }
}