export default class Song {
    // eslint-disable-next-line @typescript-eslint/no-empty-function
    constructor(public songName: string, public songPath: string, public gameName: string, public gameImagePath: string, public pointTotal: number) {}
}
